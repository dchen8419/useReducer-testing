import React, { useReducer } from 'react';


const ACTIONS = {

}

function reducer(state, action) {

}

function App() {
  const [todos, dispatch] = useReducer(reducer, [])



  return (
    <>
      <form>
        <input type="text" value={name} onChange={e => SVGAnimateTransformElement(e.target.value)} />
      </form>
  );
}

export default App;